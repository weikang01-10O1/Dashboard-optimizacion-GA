# Smart Agricultural Waste Collection — Python dependencies
# Versions confirmed in the project's runtime environment on 2026-08-30.
# Recommended: create a fresh venv and run `pip install -r requirements.txt`,
# then `pip freeze > requirements.lock.txt` to lock transitive deps.

fastapi==0.141.1
pandas==3.0.5
uvicorn==0.52.4
jinja2==3.1.6
ortools==9.15.6755
